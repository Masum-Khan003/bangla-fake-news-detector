# Development + Training dependencies
# Install with: pip install -r requirements-dev.txt
# NOT used in Docker/production

# Includes all production deps
-r requirements-prod.txt

# ── Training (GPU torch installed separately on Kaggle) ───
accelerate==1.13.0
datasets==4.8.5
scikit-learn==1.7.2
imbalanced-learn
netcal
optimum
onnx
onnxruntime

# ── Experiment Tracking ───────────────────────────────────
wandb==0.27.0

# ── Data Processing ───────────────────────────────────────
pandas==2.3.3
pyarrow==24.0.0
kaggle==1.7.4.5
dvc==3.67.1
openpyxl==3.1.5
tabulate==0.10.0
seaborn==0.13.2
matplotlib==3.10.9

# ── Jupyter ───────────────────────────────────────────────
jupyter==1.1.1
jupyterlab==4.5.7
ipykernel==7.2.0
ipywidgets==8.1.8

# ── Testing ───────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.28.1

# ── Code Quality ──────────────────────────────────────────
ruff
black
pre-commit

# ── Playwright (heavy — only for scraper fallback testing) ─
playwright==1.60.0

# ── Load Testing ─────────────────────────────────────────
locust
